[pytest]
# ASIOverlayWatchDog Test Configuration

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers
markers =
    requires_camera: Tests that require physical ZWO camera hardware
    requires_network: Tests that require internet access (weather API, web server bind)
    requires_ml_models: Tests that require ONNX models in ml/models/ to be present
    slow: Tests that take more than 1 second
    integration: Integration tests that interact with external systems

# Default options
addopts = -v --tb=short

# Warning filters
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# Logging
log_cli = false
log_cli_level = INFO
